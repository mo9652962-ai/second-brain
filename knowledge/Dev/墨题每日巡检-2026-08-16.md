# 墨题每日巡检报告 · 2026-08-16

> 只读快检（dsh --profile headless）· 未修改任何文件、未安装依赖

## 1. Git 状态 —— ⚠️ 有未提交改动

- `git status --short`：**2 处未提交改动**（均为前端，+29 / -3 行）
  - `M frontend/src/components/ContentBlocks.vue`（+14）
  - `M frontend/src/views/PracticeView.vue`（+18）
  - 内容为 **v3.6 选词填空交互**：文章空格改为可点击按钮（`blank-click` 事件 + `blankAnswers` 回填），PracticeView 增加 `isCloze` 分支；引用的 `blankAnswerText/openBlankPicker/blankQuestion` 均已有定义 ✅
- `git log --oneline -3`：
  - `854857c5` docs(readme-en): 同步 beta.15 版本号 + 功能表补全
  - `a2fd5baa` fix(frontend): sql.js 初始化 10s 超时保护防白屏（v3.5）
  - `1e2750d8` chore: gitignore 覆盖 *.bak-* 备份格式

## 2. 后端健康 —— ✅ 良好

- 入口：`backend/app/main.py` ✅ 存在（`backend/main.py` 不存在，但按项目约定入口即 `app/main.py`，符合）
- 结构完整：`app/`（config.py / database.py / main.py / schemas.py / security*）、`routers/` 24 个路由（含 diagnostic / practice / wrong / vocabulary / exam 等）、`services/` 20 个服务（含 ai_client / ai_router / diagnostic_report / fsrs_scheduler 等）
- 语法检查通过：`ast.parse` → `main.py` OK、`database.py` OK
- 数据：`backend/data/` 含 app.db / english_machine.db / question_bank.db / vocabulary.db 四库

## 3. 前端健康 —— ✅ 良好

- `frontend/src/App.vue` ✅ 存在；`frontend/src/router.ts` ✅ 存在
- `package.json` scripts：
  - `dev`: `vite --host 127.0.0.1`
  - `build`: `vue-tsc --noEmit -p tsconfig.app.json && vite build`
  - `preview`: `vite preview`
- 依赖正常：Vue 3.5.13 / vue-router 4.5 / sql.js 1.14 / lucide-vue-next / Capacitor 8.5 系列

## 4. 移动端配置 —— ✅ 良好

- `frontend/capacitor.config.ts` ✅（appId `com.moti.englishpractice`，webDir `dist`，与 vite 构建输出一致）
- `frontend/android/` ✅ 完整 Gradle 工程：app/build.gradle、gradlew(.bat)、settings.gradle、capacitor 插件目录齐全

## 发现的问题与建议（按严重度排序）

1. **⚠️ 中 — 未提交改动未收尾**：2 个文件（ContentBlocks.vue / PracticeView.vue）承载 v3.6 选词填空交互，属于半成品工作区状态；建议完成验证后尽快 commit，避免与后续改动混淆。
2. **⚠️ 中 — 三库不同步风险**：`backend/data/question_bank.db`（4,272,128 B，mtime 17:45）与 `frontend/public/question_bank.db`（4,349,952 B，mtime 16:31）大小和时间均不一致。若后端库近期有数据更新，按项目规则需**同步前端离线库 + 重新打包 APK**，否则三端数据不一致。
3. **⚠️ 低 — WAL 残留**：`backend/data/question_bank.db-wal / -shm` 存在，说明该库处于 WAL 模式（可能服务仍占用，或上次未干净关闭）。拷贝/备份该库前应先 checkpoint（或复制时连同 -wal/-shm 一起），避免拷出不完整数据。
4. **ℹ️ 提示 — data 目录积存备份**：`question_bank.db.bak-contextual / .bak-vocab-annotate / .bak-vocabfix` 3 个备份文件（已被 gitignore 覆盖，不入库），可定期清理。
5. **ℹ️ 提示 — 前端无独立 lint/test 脚本**：仅 dev/build/preview，类型检查（vue-tsc）内嵌于 build；如需 CI 门禁可考虑补 `type-check` 独立脚本。

## 总体结论

核心链路健康（后端入口/语法 ✅、前端骨架 ✅、移动端配置 ✅），当前主要风险是未提交的 v3.6 功能改动和三库数据同步节奏，无阻断性问题。

---
> 🗺️ 属于 [[MOC-Dev]] · [[Home|🏠 Home]]
