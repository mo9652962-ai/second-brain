# 墨题每日巡检 - 2026-08-17

> 来源：dsh --profile headless 只读快检（未修改任何文件、未安装依赖）
> 日期：2026-08-17（周一）

## 总览

| 模块 | 状态 |
|:---|:---|
| Git | ⚠️ 7 处未提交改动 |
| 后端 | ✅ 健康 |
| 前端 | ✅ 健康 |
| 移动端 | ✅ 健康（配置完整） |

## 1. Git 状态 —— ⚠️

- 已修改 3 处：`frontend/src/services/api-adapter.ts`、`frontend/src/services/db.ts`、`scripts/release_all.py`
- 未跟踪 4 处：`frontend/public/offline_migrations.json`、`frontend/public/question_bank.db.sim-old`、`frontend/src/services/offline-migrations.ts`、`scripts/migrate_frontend_db.py`
- 最近提交：
  - `985a5cf4` fix(frontend): merge 后修复——补回 orderingItems 定义 + QuestionExplain 接入
  - `f550d867` feat(wrong-analysis): 错题归因分析 + 题目解析批量生成（任务 B/E）
  - `4c57e65f` Merge remote-tracking branch 'origin/main'

## 2. 后端健康 —— ✅

- 入口 `backend/app/main.py` 存在，ast.parse 语法检查通过
- `app/` 下 config/database/schemas/security 齐备
- `routers/` 25 个路由 + `services/` 24 个服务，含 diagnostic / ai_router / wrong_analysis
- 数据目录含 app.db / question_bank.db / vocabulary.db 多库

## 3. 前端健康 —— ✅

- `App.vue`、`router.ts`（createWebHashHistory + routes）正常
- scripts：dev / build（vue-tsc + vite）/ preview
- 依赖：Vue 3.5 / vue-router 4.5 / Capacitor 8.5 / sql.js / lucide-vue-next

## 4. 移动端 —— ✅

- `capacitor.config.ts`（appId `com.moti.englishpractice`，webDir `dist`）
- `frontend/android/` 结构完整

## 问题与建议（按严重度）

1. **⚠️ 遗留文件混入 public**：`frontend/public/question_bank.db.sim-old` 未跟踪，`.sim-old` 疑似临时备份；public 会打进 dist 且涉及三端数据一致性，建议确认后删除或加 .gitignore。
2. **⚠️ 数据同步需确认**：改动集中在「离线迁移」链路；若同时动了词库数据，需按三库同步规则同步 `frontend/public/question_bank.db` 并重打包 APK。
3. **⚠️ 后端静态目录多版本残留**：`backend/static/` 有多个 hash 版本 index-*、api-adapter/db 两套，加 build/dist PyInstaller 残留，建议清理。
4. **ℹ️ 缺 APK 构建脚本**：`cap sync` / `gradlew assembleDebug` 未固化进 package.json scripts。
5. **ℹ️ 无独立 typecheck 脚本**：`vue-tsc --noEmit` 只内嵌在 build 里。

## 结论

核心链路（后端入口/路由、前端页面/路由、移动端配置）全部健康，无致命问题。主要风险集中在未提交的离线迁移改动与遗留文件清理，提交前建议先处理问题 1、2。