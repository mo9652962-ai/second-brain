# 墨题每日巡检报告 — 2026-08-20

## 执行方式

dsh 巡检脚本调用失败（`dsh_inspect_moti.sh` → dsh `--profile headless` 启动失败），改为手动巡检。

**dsh 失败根因**：`@deepseek-ai/dsh-timeout`、`@deepseek-ai/dsh-scope`、`@deepseek-ai/dsh-session-title-llm`、`@deepseek-ai/dsh-fs` 等依赖包缺失，导致 dsh headless profile 插件树加载失败（`ERR_MODULE_NOT_FOUND`）。不影响墨题项目。

---

## 项目状态总览

| 维度 | 状态 | 详情 |
|:---|:---:|:---|
| Git | ✅ 干净 | 无未提交变更 |
| 后端 | ✅ 健康 | 148 路由，49 表，全部正常 |
| 前端构建 | ✅ 就绪 | dist/ 33MB，142 个构建产物 |
| 数据库 | ✅ 正常 | question_bank.db 8.8MB，2132 题 |
| 移动端 | ⚪ 未检查 | Android 目录存在，需构建验证 |

---

## 详细检查

### Git 状态

- **HEAD**: `c2f32863` feat(ai-experience): v9.28 Gemini batch5 任务4 中低优先级——口语亮点报告+作文语料收录
- **未提交变更**: 无
- **最近提交**: 5 个提交均来自 v9.28 Gemini batch5（作文/精讲典藏/生词覆盖率/错题间隔重复/移动端）

### 后端健康

- **路由**: 148 条（含 diagnostic、essays、speaking、explanations 等新模块）
- **数据库表**: 49 张（含 `essay_submissions`、`speaking_sessions`、`speaking_turns`、`spaced_repetition_records`、`explain_collections`、`vocabulary_occurrences`、`vocabulary_reviews` 等新表）
- **主库**: `backend/data/question_bank.db`（8.8MB，2132 题）
- **数据量**: wrong_stats=24, diagnostic_reports=5, ai_usage=16, essay_submissions=3, speaking_sessions=5
- **注意**: `backend/data/app.db` 为 0 字节空文件——实际主库是 `question_bank.db`，此为配置设计（`DATABASE_PATH = DATA_DIR / "question_bank.db"`）

### 前端健康

- **构建产物**: dist/ 33MB，142 个文件（含懒加载路由分片）
- **视图文件**: 28 个 Vue 页面，全部存在且更新时间正常（2026-08-20）
- **路由配置**: `router.ts` 存在，路由懒加载已启用

### 配置

- **数据库路径**: `DATABASE_PATH = DATA_DIR / "question_bank.db"`（`backend/data/question_bank.db`）
- **EPM_DATA_DIR 环境变量**: 可用于打包版指定可写数据目录
- **FRONTEND_DIST**: 默认 `frontend/dist/`

---

## 关键发现

1. **dsh 巡检脚本不可用**：`dsh_inspect_moti.sh` 依赖 dsh `--profile headless`，但 headless profile 的依赖包（`dsh-timeout`、`dsh-scope` 等）未安装。建议修复：`cd /c/Users/31954/AppData/Local/hermes/node && npm install @deepseek-ai/dsh-timeout @deepseek-ai/dsh-scope @deepseek-ai/dsh-session-title-llm @deepseek-ai/dsh-fs`，或改用纯 bash 巡检脚本替代 dsh 依赖。
2. **app.db 空文件**：`backend/data/app.db` 为 0 字节，实际主库是 `question_bank.db`。如不再需要可清理。
3. **项目代码无未提交变更**：最新提交为 `c2f32863`（v9.28 Gemini batch5 任务4），项目处于稳定状态。

---

## 建议

- 无阻塞性问题，项目运行正常。
- 下次巡检建议修复 dsh 依赖，或在脚本中 fallback 纯 bash 检查。