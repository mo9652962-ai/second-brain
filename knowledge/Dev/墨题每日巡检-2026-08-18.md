# 墨题每日巡检 — 2026-08-18

> 自动巡检（dsh --profile headless），只读，未修改代码。

---

## 1. Git 状态 — ⚠️

- **未提交改动 3 处**（均为 modified，无 staged / 无 untracked）：
  - `M backend/app/database.py`
  - `M backend/app/main.py`
  - `M frontend/public/offline_migrations.json`
- **最近提交**（按时间倒序）：
  - `ba70a42b` feat(offline-migrations): 前端离线库自动迁移（任务E收尾·问题1）
  - `985a5cf4` fix(frontend): merge 后修复——补回 orderingItems 定义 + QuestionExplain 接入
  - `f550d867` feat(wrong-analysis): 错题归因分析 + 题目解析批量生成（任务 B/E）

## 2. 后端健康 — ✅

- `backend/` 结构完整：`app/`（routers/ + services/）、`data/`、`static/`、`prompts/`、`exports/`、`build/`、`dist/`
- 入口文件齐全：`main.py`、`database.py`、`config.py`、`schemas.py`、`security.py`、`security_middleware.py`，另有 `standalone_main.py`（打包入口）
- 语法检查通过：`ast.parse` 对 `app/main.py` 和 `standalone_main.py` 均 **SYNTAX OK**

## 3. 前端健康 — ✅

- `frontend/src/App.vue` ✅、`frontend/src/router.ts` ✅
- `package.json` scripts：`dev`（vite --host 127.0.0.1）、`build`（`vue-tsc --noEmit -p tsconfig.app.json && vite build`）、`preview`
- 依赖正常（Vue 3.5 + Vite 6 + vue-router 4 + sql.js + Capacitor 8.5）

## 4. 移动端配置 — ✅

- `capacitor.config.ts` 存在（appId `com.moti.englishpractice`，webDir `dist`）
- `android/` 目录存在，Gradle 工程完整：`build.gradle`、`gradlew.bat`、`settings.gradle`、`variables.gradle`、`app/build.gradle` 均就位

## 5. 发现的问题与建议（按严重度排序）

1. ⚠️ **三库同步风险（中）**：未提交改动中 `offline_migrations.json` 属前端离线库迁移数据，且与 `database.py`/`main.py` 同批改动——按项目三库同步规则，若涉及词库数据结构变更，需同步 Web 离线库并**重新打包 APK**，否则三端不一致。
2. ⚠️ **APK 构建前置依赖（低）**：`capacitor.config.ts` 的 webDir 为 `dist`，打 APK 前必须先 `vite build` 生成 dist，否则 cap sync 会打包旧产物。
3. ⚠️ **build 全量类型检查（低）**：`npm run build` 串联 `vue-tsc --noEmit`，改动前端时本地构建较慢，建议日常用 `vite build` 快速验证、提交前再跑全量。
4. ℹ️ **提交粒度**：3 处改动同属 offline-migrations 收尾批次，建议作为一次提交整理并写好 message。
5. ✅ 未发现阻塞性健康问题，无需安装任何依赖。

---

*报告由 dsh 自动巡检生成，Hermes cron 定时派发。*

---
> 🗺️ 属于 [[MOC-Dev]] · [[Home|🏠 Home]]
