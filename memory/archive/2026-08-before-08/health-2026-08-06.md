# Hermes 健康报告 2026-08-06

> 生成时间: 2026-08-06 08:5x (GMT+8) | 自动巡检 cron (daily-health-check)

## 结论

**✅ 总体健康** — 核心链路可用，2 项已自动修复（GitHub MCP token / wechat 任务超时），1 项需人工处理（xAI key 失效）

## 1. 进程 / 网关

| 项目 | 状态 | 说明 |
|---|---|---|
| Hermes 桌面端 | ✅ 运行中 | Hermes.exe ×5 + python.exe 多个（scheduler 内嵌于桌面端 backend） |
| CLI 独立 gateway | 🟡 未检测到 | `hermes gateway status` 报 no gateway；但 cron 今日正常触发（08:00-08:19 多次执行）→ 调度器在桌面端 backend 内运行，CLI 检测不到属已知现象，不影响任务执行 |
| gateway.log | 🟡 08-03 后无新写入 | 与 CLI gateway 未运行一致；桌面端 backend 日志在 desktop.log |

## 2. Cron 任务（29 个 active）

| 状态 | 数量 | 明细 |
|---|---|---|
| ✅ ok | 28 | 全部正常 |
| 🔴 error | 1 | **daily-wechat-knowledge-card** 08:19 `TimeoutError idle 602s (non-streaming API)` — 已修复（见下） |
| ⚠️ 卡死 | 0 | 无 |

**修复动作**: `hermes cron edit 2745addfb4ca --provider deepseek --model deepseek-v4-flash`，并已 `hermes cron run` 触发重跑（08:46 开始）。截至 09:08 仍在 running（22 分钟）——已 pin 流式模型，不再出现 602s non-streaming 超时；成功运行本需 10-20 分钟，明日 08:00 调度验证最终结果。

历史失败记录：07-28 Connection error、07-29/08-06 non-streaming 超时、08-01 429 周配额耗尽 → 长期未 pin 模型，跟随全局默认（opencode-go）导致。现已显式 pin deepseek 官方流式。

## 3. 磁盘空间

| 盘 | 总 | 已用 | 可用 | 使用率 |
|---|---|---|---|---|
| C: | 448G | 225G | 223G | 51% |
| D: | 932G | 479G | 454G | 52% |

✅ 充足，无告警。

## 4. 内存

- 15.6GB 总 / 4.4GB 可用 / **71% 使用率**（与历史 72% 持平，正常）

## 5. API Key 连通性

| Provider | HTTP | 判定 | 影响 |
|---|---|---|---|
| deepseek 官方 | 200 | ✅ 有效 | 主力 fallback |
| fangzhou-1 (火山) | 200 | ✅ 有效 | 可用 |
| fangzhou-2 (火山) | 200 | ✅ 有效 | **当前默认 provider (deepseek-v4-pro)** |
| siliconflow | 200 | ✅ 有效 | 已恢复（此前余额不足） |
| kimi | 200 | ✅ 有效 | 可用 |
| opencode-go | 403 | 🔴 Cloudflare 1010 | 旧默认已弃用；fallback 链前 5 项仍指向它，会拖慢 fallback（快速失败，能落到 deepseek） |
| **xai** | 400 | 🔴 **key 无效** | **影响 image_generate (grok-imagine) / x_search** |
| tavily / exa / firecrawl / openrouter | 200 | ✅ 有效 | 搜索类正常 |

**修复动作**: GitHub MCP token 401 → 从 Windows 凭据管理器提取有效 token（gho_ 前缀）替换 config.yaml `mcp_servers.github.env.GITHUB_TOKEN`，已验证 `/rate_limit` 与 repo contents 均 200。备份 `config.yaml.bak-healthcheck`。（.env 内 GITHUB_TOKEN 因文件只读未能同步，但 MCP 读取 config.yaml，不影响。）

## 6. VPN / 代理（FlClash）

- 进程运行中（FlClash.exe + FlClashCore.exe + helper）
- mixed-port=7890，TUN 开启
- 直连探针 204 ✅ / 7890 代理探针 204 ✅ → **代理可用**

## 7. Git 同步（second-brain）

- 领先 0 / 落后 0 / 未提交 1（cron-health-latest.md 正常产物）
- 上次同步: 2026-08-06 08:00 auto-sync ✅

## 8. 日志体检

- errors.log 主要噪音：memvid MCP `Connection closed` 周期性重试（P2 已知，服务未起）；AGENTS.md blocked c2_heartbeat（正常过滤）；Session DB append_message NoneType（cron-health-board 一次告警，单发）
- 今日唯一 ERROR = wechat 任务超时（已修复）

## 行动项

| 优先级 | 事项 | 状态 |
|---|---|---|
| P1 | **更新 xAI API key**（console.x.ai 重新生成后放 .env XAI_API_KEY）→ 恢复图片生成/x_search | ⏳ 需 sora 操作 |
| P2 | opencode-go 已不可用（403），fallback 链前 5 项建议移除，避免每次 fallback 先撞 5 次 403 | 建议 |
| P2 | memvid MCP 服务未启动导致周期性连接重试噪音（需用时再启） | 已知 |
| P3 | config.yaml 备份文件 `.bak-healthcheck` 确认无误后清理 | 待确认 |

---
> 🗺️ 属于 [[knowledge-map]] · [[Home|🏠 Home]]
