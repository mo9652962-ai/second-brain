# Hermes 健康巡检 2026-09-03

> 巡检时间：2026-09-03 15:47 (cron)｜结论：⚠️ 基本健康 — 主链路全通、agent 活跃、调度在跑；3 项待处理（Obsidian MCP parked / knowledge-card 撞备用 provider 余额 / 备份 provider 余额枯竭）

## 总体状态 ⚠️

Hermes 桌面端运行正常：GUI scheduler 今日 14:17 启动，agent.log 实时有 API call（本 cron 即跑在活跃链路上，model=deepseek-v4-flash provider=custom，latency 19.2s）。磁盘健康、主模型链 fangzhou-2 默认 OK。**新发现：daily-wechat-knowledge-card 今日 11:24 报 402，根因是该 job pin 在 jiyuanlvdong-2（余额枯竭）——可重指到 fangzhou-2。** Obsidian MCP 持续 parked（第 3 天），需 sora 打开 Obsidian。

## 表格

| 项 | 状态 | 详情 |
|:--|:--|:--|
| 磁盘 C: | ✅ 56% | 248G/448G，可用 200G |
| 磁盘 D: | ✅ 56% | 513G/932G，可用 419G |
| 内存 | ⚠️ 76.9% | 12.0/15.6GB，空闲 3.6GB（红线 85%） |
| cron 调度 | ✅ 运行中 | 41 任务，scheduler 今日 14:17 启动；24h: completed 40 / failed 5，无批量失败联动 |
| 主模型链路 | ✅ | fangzhou-2 OK(1.2s) 默认 + fangzhou-1 OK(2.4s) + jiyuanlvdong OK(5.7s) + dengzhen OK(10.1s) + dashscope OK + keylink OK + zhipu OK |
| 网络 | ✅ | 百度直连 200；github curl 直连/代理均 SSL 握手失败(exit 35)，但 git 实际可达（obsidian-github-sync 14:00 成功） |
| 日志 | ✅ 活跃 | agent.log/gui.log/desktop.log 今日更新；errors.log 刷屏 = Obsidian MCP parked（每 5 分钟） |

## 异常清单

### P1 — Obsidian MCP parked（持续第 3 天，errors.log 每 5 分钟刷屏）
- 端口 27123 无监听 + `MCP server 'obsidian' failed initial connection ... 502 Bad Gateway` 循环刷屏。
- = Obsidian 应用未开 或 Local REST API 插件未启。
- 影响：依赖 Obsidian MCP 的 agent 任务（知识卡片/维护/图谱）产物写入受阻；obsidian-github-sync 为 no_agent 直写，不受影响（今日 14:00 ok）。
- 建议：**sora 打开 Obsidian + 启用 Local REST API 插件，之后手动 reconnect**（parked 不会自动恢复）。

### P1 — daily-wechat-knowledge-card 撞 402（今日 11:24，根因定位）
- 该 job pin 在 `custom:jiyuanlvdong-2` + model=glm-5，而 **jiyuanlvdong-2 今日探测 HTTP 402 余额不足** → 连续失败。
- 修复：`hermes cron edit 2745addfb4ca --provider custom:fangzhou-2 --model deepseek-v4-flash`（fangzhou-2 今日 OK 1.2s）。

### P2 — deterministic-verify 9/2 21:30 报 2 项异常
- 哨兵抓出 `daily-monetization-review`（9/2 晚间 Connection error 无产物）等 2 项，均为 9/2 晚间瞬时窗口连带的产物缺失，今日主链已恢复，今晚自动重跑后自愈。

### P2 — 9/2 晚间 Connection error 窗口（已恢复）
- 每日股票深度分析 + daily-monetization-review 9/2 18:00 Connection error（pin 在 fangzhou-2，今日 OK）→ 当晚上游瞬时抖动，今日 18:00 自动重跑即可。

### P2 — 备份 provider 余额枯竭（主链不受影响）
- 402 余额不足：deepseek 官方、siliconflow、jiyuanlvdong-2；429 suspended：moonshot；403 error1010：opencode-go；403：openrouter（需 18+ 确认）；404：sensenova（模型名）；FAIL：local-qwen（本地未启，正常）、tabitoken（SSL）。
- 主链 fangzhou-2 + 备用 jiyuanlvdong 均 OK，当前不影响运行。

## 备注 / 无需处理

- **QQ/微信 gateway 通道**：gateway.log 停在 8/16（日志分流，非故障）；桌面端 scheduler 14:17 启动，agent 链路活跃，cron 运行正常。
- 内存 76.9% 偏高：Memory Compression 499MB + MsMpEng 460MB + WorkBuddy×4 ~1137MB + WeChat ~377MB + ChatGPT 308MB 常驻 → 建议关掉多余窗口。
- 历史 error 多为 8/29-31 周任务撞 429/504 配额窗口（已分析过），无需单独处理。
- github.com curl SSL exit 35：与 9/1 的 000 略不同，疑 FlClash 对 github 域名瞬时重置，git 实测可达，非故障。

## 建议动作（按优先级）

1. **打开 Obsidian + 启用 Local REST API 插件 → 解除 MCP parked**（P1，stop 刷屏）
2. **repoint daily-wechat-knowledge-card 到 fangzhou-2**：`hermes cron edit 2745addfb4ca --provider custom:fangzhou-2 --model deepseek-v4-flash`（P1）
3. 备份 provider 清理：deepseek/siliconflow/jiyuanlvdong-2/moonshot 充值或移出 fallback；openrouter 完成 18+ 确认；sensenova 修模型名（P2）
4. 关掉多余 ChatGPT/WorkBuddy/WeChat 窗口，内存回到 70% 以下（P3）

---
> 🗺️ 属于 [[knowledge-map]] · [[Home|🏠 Home]]
