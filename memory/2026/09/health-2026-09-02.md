# Hermes 健康巡检 2026-09-02

> 巡检时间：2026-09-02 15:45 (cron)｜结论：⚠️ 基本健康 — 主链路全通、agent 活跃、调度在跑；3 项待处理（Obsidian MCP parked / 备份 provider 余额枯竭 / 知识卡片产物核对）

## 总体状态 ⚠️

Hermes 桌面端运行正常：GUI scheduler 今日 11:21 启动，agent.log 实时有 API call（本 cron 即跑在活跃链路上，model=deepseek-v4-flash provider=custom，latency 6~35s）。磁盘健康、主模型链 6 条全 OK。**待处理集中在 Obsidian MCP 持续 parked 与备份 provider 余额枯竭，不影响当前主链路。**

## 表格

| 项 | 状态 | 详情 |
|:--|:--|:--|
| 磁盘 C: | ✅ 55% | 245G/448G，可用 203G |
| 磁盘 D: | ✅ 56% | 514G/932G，可用 419G |
| 内存 | ⚠️ 76.9% | 12.0/15.6GB，空闲 3.6GB（红线 85%） |
| cron 调度 | ✅ 运行中 | 41 任务，scheduler 今日 11:21 启动；24h: completed 45 / failed 4，无批量失败联动 |
| 主模型链路 | ✅ | fangzhou-2 OK(4.8s) 默认 + fangzhou-1/keylink/jiyuanlvdong-2/dengzhen/zhipu OK；dashscope(百炼) OK |
| 网络 | ✅ | 百度直连 200；github 经 curl 代理 000 为已知兼容问题，git 实际可达（obsidian-github-sync 14:00 成功） |
| 日志 | ✅ 活跃 | agent.log/gui.log 今日更新；errors.log 6 条 ERROR（Obsidian MCP 刷屏为主） |

## 异常清单

### P1 — Obsidian MCP parked（持续，errors.log 每 5 分钟刷屏）
- 端口 27123 无监听 + `MCP server 'obsidian' failed initial connection ... 400 Bad Request ... parking`，重复刷屏。
- = Obsidian 应用未开 或 Local REST API 插件未启。
- 影响：依赖 Obsidian MCP 的 agent 任务（知识卡片/维护/图谱）产物写入受阻；obsidian-github-sync 为 no_agent 直写，不受影响（今日 ok）。
- 建议：**sora 打开 Obsidian + 启用 Local REST API 插件，之后手动 reconnect**（parked 不会自动恢复）。

### P1 — 备份 provider 余额枯竭（与 9/1 同批，持续）
- 402 余额不足：deepseek 官方、siliconflow、jiyuanlvdong；403 error1010：opencode-go、tabitoken；404 模型名：sensenova；403：openrouter（需 18+ 确认）；SSL 失败：moonshot；连接拒绝：local-qwen（本地 Ollama 未启）。
- **今日 12:33 有 subagent 撞 402 INSUFFICIENT_BALANCE**（fallback 链触到低余额 provider）→ 可选项收窄。
- 主链 fangzhou-2 + fallback jiyuanlvdong-2 均 OK，当前不影响运行。

### P2 — daily-wechat-knowledge-card 产物缺失（连续第 2 天被哨兵抓出）
- deterministic-verify 9/1 21:30 报 `❌ daily-wechat-knowledge-card 无产物`。
- 该 job 今日 11:51 执行 completed / status ok，但盘上 `knowledge/cards/` 无 9/1、9/2 卡片 md → 判断该 job 产物是「准备推微信」直推交付而非落盘，哨兵期望路径可能误报。
- 建议：核对 deterministic-verify 对该 job 的期望路径；若为直推模式则从哨兵清单豁免。

### P2 — 24h 内 4 个失败明细
- deterministic-verify 9/1 21:30：sentinel 报 1 项异常（见上）。
- 项目追踪 9/1 21:00：HTTP 402 余额不足 → 今晚 21:00 自动重跑，主链已恢复。
- monthly-skill-usage 9/1 12:15：HTTP 402 → 月度任务，10/1 再跑。
- 闲鱼提醒 9/1 09:59：HTTP 504 瞬时超时 → 9/2 11:30 已正常。

## 备注 / 无需处理

- **QQ/微信 gateway 通道**：gateway.log 停在 8/16（日志分流，非故障）；今日 11:21 errors.log 有 weixin send 限流记录（cooldown 30s 正常重试），通道仍活动。
- 内存 76.9% 偏高：MsMpEng 490MB + Memory Compression 475MB + WorkBuddy 455MB + ChatGPT×3 ~821MB + steamwebhelper 219MB 常驻 → 建议关掉多余窗口。
- 历史 13 个 error 多为 8/29-31 周任务撞 429/504 配额窗口（已分析过），无需单独处理。
- curl -x 7890 访问 github.com/api/raw 返回 000：**已知 curl+HTTP 代理兼容问题**（9/1 已验证），git 实测可达，非故障。

## 建议动作（按优先级）

1. **打开 Obsidian + 启用 Local REST API 插件 → 解除 MCP parked**（P1，stop 刷屏）
2. 备份 provider 清理：deepseek/siliconflow/jiyuanlvdong 充值或移出 fallback；openrouter 完成 18+ 确认；sensenova 修模型名（P1）
3. 核对 daily-wechat-knowledge-card 产物路径 vs 哨兵期望（P2）
4. 关掉多余 ChatGPT/steam/WorkBuddy 窗口，内存回到 70% 以下（P3）

---
> 🗺️ 属于 [[knowledge-map]] · [[Home|🏠 Home]]
