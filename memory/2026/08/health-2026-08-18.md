# Hermes 健康巡检 2026-08-18

结论：⚠️ 核心正常，代理异常 → provider 检查被误导为全 FAIL（假警报），消息网关通道冻结

## 总体状态
- Agent 核心：✅ 活跃（agent.log 今日 12:07 有 API 调用，provider=custom:jiyuanlvdong 直连正常）
- Provider 连通性：✅ 直连全通（deepseek 401=可达需鉴权，tokenrhythm 401=可达）
- Cron 任务：⚠️ 26 ok / 12 error（多为 8-17 代理挂时的 Connection error + HTTP 429 限流）
- 磁盘：✅ C: 52%（218G 可用）D: 60%（378G 可用）
- 内存：✅ 15.6G 总量，已用 10.5G / 67.3%
- 端口代理：❌ **FlClash 7890 损坏**（端口 LISTENING 但流量不通，curl 走它 = 000/exit 56）

## 关键发现
1. **FlClash 7890 代理失效（本轮根因）**：FlClashCore.exe (PID 30756) 监听 7890 端口，但经它代理的 curl 全部失败（连接被重置）。health_provider_check.py 脚本统一走 127.0.0.1:7890，故全 FAIL 是**假警报**。直连实测：deepseek 401、tokenrhythm(jiyuanlvdong) 401、baidu 200 —— 外网与 provider 均可达。当前 cron 正是通过 jiyuanlvdong 直连成功运行的，佐证 provider 健康。
2. **通道网关冻结**：gateway.log 自 8-16 02:24 无新输出（QQBot max reconnect / weixin poll error），消息通道（QQ/微信）疑似离线，不影响 cron/agent。
3. **昨日 5 项产物缺失（哨兵 8-17 21:30 记录）**：daily-health-check / obsidian-maintenance / daily-todo-executor / daily-wechat-knowledge-card 当日无产物。
4. **限流**：daily-todo-executor(HTTP 429 rpm)、每日仓库+知识库优化(HTTP 429 tpm)——第三方免费层限流，非代码问题。
5. **缓存脚本缺失**：cache-hit-monitor 报 "Script not found: cache_hit_monitor" —— 该 cron 关联脚本已不存在或路径错。

## 异常项清单 + 建议
| 项 | 状态 | 建议 |
|---|---|---|
| FlClash 7890 代理 | ❌ 损坏 | **重启 FlClash** 恢复代理；gateway 通道依赖它 |
| health_provider_check 假警报 | ⚠️ | 先 curl 实测 7890 再判读；或脚本改直连 |
| 通道网关冻结 (QQBot/weixin) | ⚠️ | 重启 FlClash + 重启 gateway 观察 reconnect |
| cache-hit-monitor 脚本缺失 | ❌ | 检查 cron 该 job 引用的脚本路径/重建 |
| 限流 429 (todo/仓库优化) | ⚠️ | 属第三方免费层限流，重试即可，无需改码 |
| 昨日 4 项 cron 无产物 | ⚠️ | 见哨兵异常明细，逐项手动补跑 |

---
> 🗺️ 属于 [[knowledge-map]] · [[Home|🏠 Home]]
