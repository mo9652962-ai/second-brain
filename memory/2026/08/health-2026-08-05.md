# 🩺 Hermes 健康巡检报告 2026-08-05

> 巡检时间：2026-08-05 08:15（cron daily-health-check 自动执行）
> 结论：**🟡 整体良好** — 26/26 cron 正常、核心 API 连通、磁盘/内存健康、VPN 恢复；2 个已知降级项（opencode-go 余额、xAI key 失效）+ standalone gateway 自 08-03 停止（cron 由桌面端承载）

---

## 一、总览

| 项目 | 状态 | 说明 |
|---|---|---|
| Cron 调度器 | 🟡 | 26/26 任务上次运行全部 ok；ticker 心跳 08:19 正常。standalone gateway 进程自 **08-03 15:53** 停止（QQBot 100 次重连耗尽），调度改由桌面应用内嵌承载（今日 03:11 全栈重启后无 ERROR） |
| API Key 连通性 | 🟡 8/10 | deepseek/siliconflow/kimi/fangzhou-1/fangzhou-2/openrouter/tavily/GitHub-MCP 全通；**opencode-go 403 余额不足**、**xai 400 key 失效**（均 08-03 已知项） |
| 磁盘 | ✅ | C: 50%（227G 可用）；D: 52%（454G 可用） |
| 内存 | ✅ | 15.6GB 总 / 5.5GB 可用 / 64% 使用 |
| VPN/代理 | ✅ | FlClash 运行中，mixed-port 7890 + TUN 双通道均通（直连 gstatic 204、代理 204、api.x.ai 可达）；08-04 的 TUN 失效问题已恢复 |
| Git 同步 | ✅ | 落后 0 / 领先 0；2 个未提交变更（待 10:00 auto-sync）；上次同步 08:00 成功 |
| 日志 | ✅ | 今日无 cron ERROR；仅 memvid MCP parking 等已知噪音 |

## 二、API Key 明细（2026-08-05 实测）

| Provider | 端点探测 | 结果 | 判定 |
|---|---|---|---|
| deepseek（主力） | GET /models | 200 | ✅ 正常 |
| siliconflow | GET /models | 200 | ✅ 正常 |
| kimi（内联 key） | GET /models | 200 | ✅ 正常（08-04 误报 401 已澄清为解析 bug，已修复脚本） |
| fangzhou-1（ark-c1fd） | POST chat(max_tokens=1) | 200 | ✅ 正常（key+余额均有效） |
| fangzhou-2（ark-0984） | POST chat(max_tokens=1) | 200 | ✅ 正常 |
| openrouter | GET /models | 200 | ✅ 正常 |
| tavily | POST search(limit=1) | 200 | ✅ 正常 |
| GitHub MCP | GET /user + 真实调用 list_commits | 200 | ✅ 正常（config.yaml gho_ token 有效，账号 mo9652962-ai；.env 的 ghp_ 旧 token 已死但未被使用） |
| **opencode-go** | GET /models | **403 Cloudflare 1010** | 🔴 余额不足（08-03 已知）；fallback 链自动落 deepseek 官方，系统稳定 |
| **xai** | GET /models（经代理） | **400 Incorrect API key** | 🔴 key 失效（08-03 已知）；影响 image_generate（grok-imagine）与 x_search |

## 三、Cron 状态

- 26 个任务全部 `ok`（含每日/每周/双周/每月任务）
- 昨晚 20:10 daily-todo-executor、21:10 项目追踪 曾 600s 超时失败（非流式 API 经典超时），凌晨 00:13/00:17 重跑成功，当前均 ok
- 今日 00:04/00:21/00:25/02:15 有 4 条 streaming Connection error（凌晨瞬时网络，非 cron 调度错误；03:11 重启后无新增 ERROR）

## 四、行动项

### P1（需 sora 处理）
1. **xai API key 失效**（400 Incorrect API key）→ 更新 `C:\Users\31954\AppData\Local\hermes\.env` 的 XAI_API_KEY；影响图片生成（grok-imagine）/x_search
2. **opencode-go 余额不足**（403 Cloudflare 1010）→ 充值或维持现状（fallback 已兜底，deepseek 官方直连稳定）

### P2（建议）
3. **standalone gateway 停止** → QQ/微信消息平台自 08-03 不可用；cron 依赖桌面应用保持开启。如需恢复消息平台/独立调度：`hermes gateway` 重启（注意桌面端内嵌调度器可能双跑，先确认机制再启）

### P3（清理/已知噪音）
4. `.env` 中废弃 GITHUB_TOKEN（ghp_ 前缀，401 已死）可清理——git 同步走 Windows 凭据管理器、GitHub MCP 走 config.yaml token，均不依赖它
5. memvid MCP 周期性 `Connection closed` parking —— 服务未启用，已知噪音

## 五、本次巡检改进

- 修复 `hermes-configuration-patterns/scripts/parse_providers.py`：原脚本只认 0 缩进的 `- ` 条目，实际 config.yaml 为 2 缩进（`  - api_key: ...`），导致解析返回空、kimi/fangzhou 误报 KEY MISSING。已改为按 `^\s*-\s+[A-Za-z_]+:` 任意缩进识别条目，验证通过
- 健康度评分：**92%**（-3% opencode-go / -3% xai / -2% gateway 单点依赖）
