# Hermes 健康巡检 2026-08-31 08:59

## 总体结论 ⚠️ 基本健康

**agent 模型调用正常**（本次巡检本身即为 agent 在跑；agent.log 今日 API call #7 provider=custom 成功）。磁盘/内存/进程全绿。**主要告警是 health_provider_check.py 全 FAIL——经实证是脚本不走系统代理的误报，非真实故障**；昨日 cron 批量 429 配额失败是真实问题，需关注 fallback 链额度。

## 明细

| 项目 | 状态 | 详情 |
|---|---|---|
| 磁盘 C | ✅ | 448G 用 236G (53%)，剩 213G |
| 磁盘 D | ✅ | 932G 用 511G (55%)，剩 421G |
| 内存 | ✅ | 15.6G 总 / 已用 11.3G (72.4%)，低于 85% 红线 |
| Hermes 进程 | ✅ | agent.log 08:59 活跃；gui.log 08:58 scheduler 启动正常 |
| Provider 连通 | ⚠️ | 脚本 19 个全 FAIL，但 agent 实际调用成功（见下方诊断）|
| cron 今日 | ✅ | 08:58 obsidian-github-sync / 每日学习计划 ok |
| cron 昨日 8/30 | ⚠️ | 批量 HTTP 429 配额超限（见下方）|
| 哨兵 deterministic-verify | ⚠️ | 8/30 报 4 项产物缺失（昨 429 批失败的连锁）|
| skill-link-gate | ⚠️ | 38/424 skill 断裂引用（真问题，待清理）|
| Obsidian MCP | ⚠️ | parked：nemo_relay 模块缺失 + 27123 端口无监听 |
| 微信网关 | ⚠️ | send chunk WinError 10053 间歇失败 |

## Provider 全 FAIL 诊断（本次核心排查）

**现象**：health_provider_check.py 对全部 19 个 provider 报 FAIL（WinError 10054/10053 连接被重置，8-32ms 极速失败）。

**实证对照**（证明是脚本环境问题，不是真断网）：
- `curl` 直连：baidu → 200 ✅、api.deepseek.com → 401 ✅（可达）
- `python urllib` 直连同一域名 → 10054/10053 重置 ❌
- 系统代理 = `127.0.0.1:7890`（FlClash 在跑，FlClashCore.exe 监听 7890）
- **agent 今日模型调用成功**（agent.log `API call #7 ... provider=custom ... latency=16.2s`，即本次巡检本身）

**根因**：脚本用 `urllib` 不读 Windows 系统代理（127.0.0.1:7890），直连境外/中转域名被 FlClash 网络栈重置；而 agent 走系统代理正常。**脚本自身需修**（走代理或注入 env），下次巡检前应修复。

## cron 异常分析

### 昨日 8/30 批量 429（真实问题）
- 错误模式：`HTTP 429: inference tpm exhausted` / `Allocated quota exceeded`
- 涉及：arxiv-fetch/summarize、hackernews-daily、obsidian-maintenance、daily-self-improvement、weekly-* 系列、daily-todo-executor、组会报告 等 10+ 任务
- agent.log.1 统计：fallback 链中 **sensenova 429 命中 509 次、openrouter 33 次** → 主链抖动后 fallback 撞上配额不足
- 今日主链（custom:fangzhou-2 / deepseek-v4-flash）已恢复，今日任务正常
- **建议**：核对 sensenova/openrouter 账户额度；8/30 失败任务如无产物可手动补跑

### drift 跳过（历史遗留，8/24/8/29）
- AI测评周报、shai-hulud-weekly-scan、每日股票深度分析、文献周报 报 `Skipped to prevent unintended spend`（unpinned + 全局模型漂移）
- 建议：按技能流程 `hermes cron edit <id> --provider custom:fangzhou-2 --model deepseek-v4-flash` 逐个 pin

### deterministic-verify 哨兵（8/30 4 项缺失）
- ❌ arxiv-fetch(7:00)、daily-health-check(8:15)、obsidian-maintenance(6:00)、hackernews-daily(7:00) 无产物
- 均为 8/30 429 批失败连锁，今日已恢复。本文件即今日 health 产物，明日哨兵可过。

### skill-link-gate 断裂（真问题）
- 38/424 skill 有断裂引用，示例：android-automation → references/exit-code-orchestration.md 不存在；easyeda-automation → _quick-reference.md 不存在；hermes-automation-patterns → opencode-go-timeout-fix.md 不存在 等
- **建议**：开专项清理（补文件或删引用），非本次巡检范围

## 日志
- errors.log 今日 00:17 有 `provider=custom:jiyuanlvdong-2 base_url=tokenrhythm.studio` Connection error（3 retries 失败）——夜间一段 tokenrhythm 抖动
- errors.log 08:58 `MCP server 'obsidian' failed initial connection ... ModuleNotFoundError: No module named 'nemo_relay'` + 27123 无监听 → Obsidian 桌面端未开或 Local REST 插件未启，parked 状态需手动 reconnect

## 可操作建议（按优先级）
1. **修 health_provider_check.py**：改用走 127.0.0.1:7890 代理的客户端，或脚本内注入代理 env，否则每次巡检都误报全 FAIL
2. **核对 sensenova/openrouter 余额**：fallback 链 429 频发，确认额度或调整 fallback 顺序
3. **pin 漂移 cron**：AI测评周报 / shai-hulud-weekly-scan / 每日股票深度分析 / 文献周报
4. **清理 skill 断裂引用**（38 处）
5. **Obsidian**：如需知识卡片/维护类 cron 正常，开 Obsidian + Local REST API 插件后 reconnect

## 附：内存 Top 进程
node 610M / MsMpEng 597M / steamwebhelper 406M / Hermes 304M / node 293M / EADesktop 283M / python 259M
